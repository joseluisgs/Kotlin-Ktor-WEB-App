{{> header.hbs }}

<section class="section">
    <div class="container">
        <div class="notification is-primary">
            <h1>{{pageTitle}}</h1>
            <h2>Hola, {{user.firstName}} {{user.lastName}}</h2>
        </div>
    </div>

{{> footer.hbs }}
</section>